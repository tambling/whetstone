.resource{ class: "#{resources_stone.resource.difficulty} All", data: {row: 1, col: 1, sizex: 1, sizey: 1, id: "#{resources_stone.id}", save_url: "#{saved_resources_path}"} }
  %li
    %h4= resources_stone.resource.title
    %a.upvote{id: "#{resources_stone.id}"} Upvote
    %a.downvote{id: "#{resources_stone.id}"} Downvote
    %p= "#{resources_stone.votes} Votes"
    %p= resources_stone.resource.description
    %p= resources_stone.resource.recommended_time
    %p= resources_stone.resource.difficulty
