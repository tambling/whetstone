#stone{data: {user_id: "#{current_user_id}", stone_id: stone.id}}
  %h2= stone.title
  %h3= stone.description
  - if goal.nil?
    %button#add_goal Add Goal
  - else
    .resources.sortable
      = render partial: 'resources/resources_stone', collection: goal.resources_stones, as: :resources_stone
